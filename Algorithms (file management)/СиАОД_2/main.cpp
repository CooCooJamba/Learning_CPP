#include "functionsFile.h"
#include "functionsFileBinary.h"
int main() {
	setlocale(LC_ALL, "ru");
	//Первая часть
	/*cout << "Операция создания файла." << endl;
	createFile();
	cout << endl << "Операция чтения файла." << endl;
	readFile();
	cout << endl << "Операция добавления записи в файл." << endl;
	addValueFile();
	cout << endl << "Операция поиска записи в файле." << endl;
	cout << "Введите порядковый номер числа, чтобы его найти." << endl;
	int index;
	cin >> index;
	cout << "Число по номеру " << index << ": " << findValueFile(index) << " найдено." << endl;
	cout << endl << "Операция подсчёта записей в файле." << endl;
	cout << "Всего чисел в файле: " << countValuesFile() << "." << endl;
	cout << endl << "Операция удаления записи из файла." << endl;
	cout << "Введите порядковый номер числа, чтобы его удалить." << endl;
	int i;
	cin >> i;
	cout << "Число по номеру " << i << ": " << (deleteValueFile(i) ? "успешно" : "не") << " удалено." << endl;
	cout << endl << "Проверка удаления записи из файла." << endl;
	readFile();*/
	//Вторая часть
	/*readSessionFile();
	cout << endl;
	createBinaryFile();
	cout << endl;
	readBinaryFile();
	cout << endl;
	createPoster();
	cout << endl;
	readBinaryFile();
	cout << endl;
	increaseCost();
	cout << endl;
	readBinaryFile();
	cout << endl;
	deleteEntry();
	cout << endl;
	readBinaryFile();
	cout << endl;
	fileWriteAccess();
	cout << endl;
	readBinaryFile();
	cout << endl;*/
	return 0;
}